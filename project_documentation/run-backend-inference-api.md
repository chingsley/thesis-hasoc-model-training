# Run the backend inference API

This guide is merged into **[run-dashboard.md](./run-dashboard.md)** (backend + frontend + browser access).

For backend-only `curl` testing, see the **Quick API test** section there.
